import { returnTags } from 'unitags'; // Make sure this path matches the actual exported path

async function test() {
  try {
    const tags = await returnTags();
    console.log(tags);
  } catch (error) {
    console.error(error);
  }
}

test();
