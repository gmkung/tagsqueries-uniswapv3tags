const unitags = require("unitags"); // Replace with your actual package name

// Use the exported function
unitags
  .fetchAllPools()
  .then((pools) => {
    console.log(pools);
  })
  .catch((error) => {
    console.error(error);
  });
